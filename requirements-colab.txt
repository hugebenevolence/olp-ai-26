# Keep Colab's CUDA-matched torch/torchvision/numpy stack intact.
# These are the small packages used by the baseline notebooks that are not always preinstalled.
timm>=1.0,<2
sacrebleu>=2.4,<3
rouge-score>=0.1.2,<0.2
seqeval>=1.2,<2
